{
  "name": "frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "cd auth-frontend && pnpm start",
    "build": "cd auth-frontend && pnpm run build",
    "install": "cd auth-frontend && pnpm install"
  }
}
